server.port=${SERVER_PORT:8080}
server.shutdown=graceful

spring.application.name=farm-to-home-api
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/farm_to_home}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Gopi@786}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.hikari.maximum-pool-size=${DB_POOL_SIZE:10}
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=20000

spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=none
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration

spring.jackson.default-property-inclusion=non_null

spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:mail.farmtohomef@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:ozwykgdylurdgsqb}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000

management.endpoints.web.exposure.include=health,info
management.endpoint.health.probes.enabled=true

app.firebase-project-id=${FIREBASE_PROJECT_ID:farm-to-home-8c520}
app.cors-origins=${CORS_ORIGINS:http://localhost:*,http://127.0.0.1:*}
app.mail-from=${MAIL_FROM:mail.farmtohomef@gmail.com}
app.resend-api-key=${RESEND_API_KEY:}
app.resend-from=${RESEND_FROM:onboarding@resend.dev}

logging.level.root=INFO
logging.level.org.springframework.security=WARN
